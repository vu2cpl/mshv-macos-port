<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
    Hardened-runtime entitlements for the MSHV macOS bundle.

    com.apple.security.cs.disable-library-validation
        Required because we ship our own Qt 5 frameworks and PortAudio /
        FFTW dylibs inside Contents/Frameworks/. Those are signed with the
        same Developer ID as the main binary, but they aren't signed by
        Apple. The hardened runtime would refuse to load them otherwise.

    com.apple.security.device.audio-input
        Microphone access. The user-facing prompt comes from
        NSMicrophoneUsageDescription in Info.plist; this entitlement is
        what permits the prompt to appear under the hardened runtime.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
</dict>
</plist>
